#include <stdio.h>
   
   int main(){
     int result;
     int num;
     printf("Enter the positive number: ");
     scanf("%d", &num);
     result = num << 1;
     printf("%d is the multiple of positive number", result);
    return 0;
  }                    
